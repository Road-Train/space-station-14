name: Auto Assign Reviewers

on:
  pull_request:
    types: [labeled, opened, synchronize]

jobs:
  assign:
    runs-on: self-hosted
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - name: Assign reviewers based on labels
        uses: actions/github-script@v7
        with: # TODO AFTERLIGHT assign different maints here
          script: |
            const labelToAssign = {
              "Changes: Sprites": ["DrSmugleaf"],
              "Changes: Map": ["DrSmugleaf"],
              "Changes: UI": ["DrSmugleaf"],
              "Changes: Shaders": ["DrSmugleaf"],
              "Changes: Audio": ["DrSmugleaf"],
              "Changes: No C#": ["DrSmugleaf"],
            };

            const labels = context.payload.pull_request.labels.map(label => label.name);
            const assignees = new Set();

            for (const label of labels) {
              const tempAssing = labelToAssign[label];
              if (tempAssing) {
                tempAssing.forEach(a => assignees.add(a));
              }
            }

            if (assignees.size > 0) {
              await github.rest.issues.addAssignees({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                assignees: Array.from(assignees),
              });
            }
